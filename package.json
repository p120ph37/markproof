{
  "name": "markproof",
  "version": "1.0.0",
  "description": "Trust-anchored web application loader — secure against server compromise, domain seizure, and MITM attacks",
  "type": "module",
  "scripts": {
    "build": "bun run build.ts",
    "keygen": "bun run src/plugin/keygen.ts",
    "dev": "bun run --watch build.ts",
    "preview": "bunx serve dist"
  },
  "devDependencies": {
    "@types/bun": "^1.0.0"
  }
}
